<script src="https://cdn.jsdelivr.net/npm/@algolia/autocomplete-js@VERSION/dist/umd/index.production.js" defer></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@algolia/autocomplete-theme-classic@VERSION/dist/theme.min.css">



<script defer>
  document.addEventListener('DOMContentLoaded', function() {
    const searchClient = algoliasearch('testingMUEWDUHCI5', '6c021d2e41d9adb957e13dd95fc93581');
    const search = instantsearch({
      indexName: 'qa-marlinn_products',
      searchClient,
    });

    search.addWidgets([
      instantsearch.widgets.searchBox({ container: '#searchbox' }),
      instantsearch.widgets.hits({ container: '#hits' }),
    ]);

    search.start();
  });
</script>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const searchClient = algoliasearch('YOUR_APP_ID', 'YOUR_SEARCH_ONLY_API_KEY');

    autocomplete({
      container: '#autocomplete-container',
      placeholder: 'Search...',
      openOnFocus: true,
      getSources({ query }) {
        return [
          {
            sourceId: 'products',
            getItems() {
              return getAlgoliaResults({
                searchClient,
                queries: [
                  {
                    indexName: 'your_index_name',
                    params: {
                      query: query,
                      hitsPerPage: 5
                    },
                  },
                ],
              });
            },
            templates: {
              item({ item }) {
                return `<div>${item.title || item.name}</div>`;
              },
              noResults() {
                return 'No results found';
              },
            },
          },
        ];
      },
    });
  });
</script>